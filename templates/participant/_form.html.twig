<div style="text-align: center;background-color: #ebebeb;border-radius: 10px;width: 32%;margin-left: 10%;height: 470px;margin-left: auto;margin-right: auto;">
    <div>
    <h1>Mon Profil</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}<br>
    <button class="btn" style="border-radius: 6px;
            width: 20%;
            background-color: #3a57af;
            color: #ebebeb;
            border-color: #ebebeb;">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
    </div>

</div>